spring:
  application:
    name: USER-SERVICE
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      name: USER-SERVICE

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/             