spring:
  application:
    name: USER-SERVICE
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      name: USER-SERVICE

#eureka:
#  instance:
#    hostname: localhost
#  client:
#    enabled: true
#    register-with-eureka: true
#    fetch-registry: true
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka             